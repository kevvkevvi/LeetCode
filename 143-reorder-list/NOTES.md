<div> Use a slow and fast pointer to find the dividing point of the linked list. Then, reverse the second half of the linked list. Afterwards, alternate merging from each linked list.

O(n), O(1)</div>
